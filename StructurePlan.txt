Here is all the content of the index.html:

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>pomotrack 🍅</title>
    <link rel="stylesheet" href="style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>

<nav class="fixed top-0 w-full py-12 px-20 flex items-center justify-between">
  <div class="text-[40px] font-bold pacifico">
    pomotrack
  </div>

  <div class="toggle-modes relative flex items-center h-12 rounded-[20px] border-[1.5px] bg-[#F8F3F3] overflow-hidden shadow-[4px_3px_3px_1px_rgba(0,0,0,0.25)]">
    
    <button class="light-mode flex items-center justify-center w-[72px] h-12 transition">
      <img src="sun.svg" alt="Light mode" class="w-6 h-6">
    </button>

    <div class="w-px border h-full"></div>

    <button class="dark-mode flex items-center justify-center w-[72px] h-12 hover:bg-[#B5B5B5] transition">
      <img src="moon.svg" alt="Dark mode" class="w-6 h-6">
    </button>

  </div>
</nav>

  <main class="main-content mt-0 flex flex-col items-center">
    <div class="pomo-options manrope flex justify-center gap-16 mt-50 text-lg font-medium">
        <div class="pmo-btn focus">Focus</div>
        <div class="pmo-btn break">Break</div>
        <div class="pmo-btn cycle">Cycle</div>
    </div>
    <div  contenteditable="true" id="timerDisplay" class="timer-display flex justify-center items-center mt-16 text-[120px] manrope">
      25:00
        <!-- Timer Display Placeholder -->
    </div>
    <p class="text-help text-gray-500 text-sm">Click to edit & Use cursor keys to navigate.</p>
    <div class="options flex flex-wrap justify-center gap-16 mt-20 text-lg">
          <button id="startButton" class="start-stop flex items-center justify-center border-2 bg-[#F8F3F3] px-10 py-3 rounded-[30px] shadow-[5px_4px_4px_2px_rgba(0,0,0,0.25)] w-[160px] text-center hover:bg-[#B5B5B5] hover:font-semibold cursor-pointer transition-colors duration-150">Start</button>
        <div class="reset flex items-center justify-center w-12 h-12 cursor-pointer">
            <img src="reset.svg" alt="Reset" class="w-6 h-6">
        </div>
        <div class="settings flex items-center justify-center w-12 h-12 cursor-pointer">
            <img src="settings.svg" alt="Settings" class="w-6 h-6">
        </div>
    </div>
  </main>
  <footer class="footer flex gap-8 justify-center fixed bottom-6 w-full italic">
      <div class="support hover:underline"><a href="https://youtu.be/m7gCn9u9bM4?si=nscmbRp__Vp5t5R5">support</a></div>
      <div class="github hover:underline"><a href="https://github.com/llamma07/pomotrack">github</a></div>
  </footer>
  <script src="dark-mode.js"></script>
  <script src="timer-input.js"></script>
  <script src="focus.js"></script>
  <script src="break.js"></script>
  <script src="cycle.js"></script>
  <script src="app.js"></script>
  

</body>
</html>

I need to now work on the Javascript to make it all work. 
Create the full JavaScript functionality for my timer app.
I would prefer the JavaScript to be split cleanly into differnt files for different function such as:

* `focus.js`
* `break.js`
* `cycle.js`
* `app.js` (main controller that connects everything)

The code must be efficient, modular, and bug-free.

### TIMER DISPLAY EDITING

The timer display (MM:SS format) must be clickable and editable.

**Editable positions:**

* Position 0 → first digit of minutes
* Position 1 → second digit of minutes
* Position 2 → ":" (NOT editable, must be skipped)
* Position 3 → first digit of seconds
* Position 4 → second digit of seconds

**Navigation rules:**

* User can navigate using mouse click or arrow keys.
* Clicking should snap cursor to nearest valid editable position (0,1,3,4).
* Do NOT force cursor to start at position 0.
* Arrow right: move forward through valid positions.
* Arrow left: move backward.
* If moving forward from position 4 → loop to 0.
* If moving backward from position 0 → loop to 4.
* Skip position 2 always.
* Only numeric input allowed.
* Do NOT allow adding extra characters or leaving MM:SS format.

### VALIDATION RULES

* Maximum allowed time: **59:59**

  * If exceeded → show text: **"Maximum time is 59:59"**
  * Prevent invalid entry.
* If user enters **00:00**

  * Show: **"Timer must be greater than 00:00"**
* Timer must always remain valid MM:SS.

### HOVER TEXT

When user hovers over timer display:
Show helper text from `text-help` element:

**"Click to edit & Use cursor keys to navigate."**

Show only while hovering.

### RUNNING TIMER RESTRICTION

If timer is currently running and user clicks timer:
Show message:

**"Timer can't be changed when running."**

Do NOT allow editing while running.

So for all the validation rules, the text-help class component can be used. When this element fades into
the page, it should not cause the buttons to be pushed down. Also it should fade out after after 
a suitable standard time after which the viewer has seen the notification. 
Though, if the user immediately hovers over the timer display, the hover text should reappear.

### TAB SYSTEM

There are 3 tabs:

* Focus
* Break
* Cycle

Each tab must store its own independent time.

**Default values:**

* Focus → 25:00
* Break → 5:00
* Cycle → 25:00 for Focus and 5:00 for Break

If user changes a tab’s time:

* Save that custom value.
* When switching away and returning → restore last custom value.
* Each tab must remember its own time independently.

Example:

* Focus set to 30:00
* Switch to Break → shows default 5:00 (or last custom break)
* Return to Focus → shows 30:00

### TIMER BEHAVIOR WHEN SWITCHING TABS

If timer is running in one tab and user switches tabs:

Example:

* Focus running at 23:22
* Switch to Break tab

Then:

* Focus timer must automatically pause at 23:22.
* Break tab should show its own last saved time (ex: 10:00).
* When returning to Focus → it remains paused at 23:22.
* User can press start to resume.

This behavior must apply to:

* Focus
* Break
* Cycle

Each tab has its own paused state and time.

### CYCLE MODE LOGIC

Cycle mode should:

* Run Focus timer
* When Focus ends → wait 1.5 seconds
* Then automatically start Break
* Continue cycle logic

Here's the HTML code associated with the Cycle tab as it has some extra functionality. So when the Cycle 
tab is pressed, this code should be rendered - 
 <div class="cycle-timers flex justify-center gap-32 mt-10">
        <div class="flex flex-col items-center">
          <label class="text-lg font-medium mb-4">Focus</label>
          <div contenteditable="true" id="focusTimer" class="timer-display text-[120px]">25:00</div>
        </div>

        <div class="flex flex-col items-center">
          <label class="text-lg font-medium mb-4">Break</label>
          <div contenteditable="true" id="breakTimer" class="timer-display text-[120px]">05:00</div>
        </div>
      </div>

      <div class="cycle-controls flex justify-center items-center gap-4 mt-8">
        <span class="text-lg font-medium">Cycles:</span>
        <button id="decreaseCycle">−</button>
        <input id="cyclesInput" value="4" readonly class="w-12 text-center">
        <button id="increaseCycle">+</button>
      </div>

      <div class="cycle-progress mt-6 text-center text-lg font-medium">
        <span id="currentCycleDisplay">Cycle 1 of 4 - Focus</span>
      </div>

Include smooth transition and ensure no timing bugs.

### GENERAL REQUIREMENTS

* Clean modular JS across the 4 files.
* `app.js` controls tab switching, shared state, and UI behavior.
* Each module manages its own timer logic.
* Ensure no memory leaks or multiple intervals running.
* Ensure editing, saving, pausing, switching tabs all work perfectly.
* Optimize for performance and clarity.

Return complete working JS split into:
focus.js
break.js
cycle.js
app.js
